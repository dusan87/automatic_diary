<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elfak.automatic_diary"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme"
        android:debuggable="true"
        android:name=".MyApplication">
        <activity
            android:name="com.elfak.automatic_diary.activities.LoginActivity"
            android:label="@string/app_name"
            android:configChanges="screenSize|orientation|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegistrationActivity"
            android:label="@string/registration_label">

        </activity>
        <activity
            android:name=".activities.RegistrationActivity_1"
            android:label="@string/registration_label">

        </activity>

        <activity
            android:name=".activities.UsersListActivity"
            android:label="Users List">
        </activity>

        <meta-data
            android:name="com.crashlytics.ApiKey"
            android:value="ddc6da61d6ce0908a0076c30f6b620c2aad4e160" />
    </application>

</manifest>
